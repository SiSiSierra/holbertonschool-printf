# holbertonschool-printf

School project to recreate C's printf function

- count number of characters printed `int count`
- cycle through character string
- check
- if character = "%", check next character
- if character = "\", check next character
- write each character
- increment count
- stop when \0 is reached
